#include <stdio.h>
#include <stdlib.h>

int main(int argc, char **argv)
{
	/*int i, j;
	float p;
	if(argc != 2){
		printf("Wrong arguments");
	}
	FILE *fp = fopen(argv[1], "rt");
	float conf_mat[q][q];

	for(i = 0; i < q; i++){
		for(j = 0; j < q; j++){
			fscanf(fp, "%f", &p);
			conf_mat[i][j] = p;
		}
	}

	for(i = 0; i < q; i++){
		for(j = 0; j < q; j++){
			printf("%f", conf_mat[i][j]);
		}
	}*/

	char *s = "tyt";
	int x = strlen(s);
	printf("%s", s);

	return 0;
}